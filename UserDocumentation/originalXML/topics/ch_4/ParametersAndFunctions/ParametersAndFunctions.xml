<?xml version="1.0" encoding="UTF-8"?>
<vcelldoc>
<page title="Parameters and Functions">

	
	<introduction>
	Users can define parameters to aid in modeling their virtual experiments or <link target = "ApplicationOverview">applications</link>.
	User-defined parameters can be added at the physiology level either as <bold>global</bold> parameters or as <bold>reaction</bold> parameters.
	There are other predefined parameters such as species initial conditions, structure sizes or reaction rates for the different kinetic types that are generated as the model is being developed, for which the user has to provide values. 
	The parameter values can be numeric or expressions involving other allowable variables in the model. Parameter values that are expressions are referred to as <bold>Functions</bold> in the Virtual Cell. 
	</introduction>
	
	<appearance>
		<imgReference target = "ParamsAndFns.png"/>  
	</appearance>
	
	<operations>
	<para> <bold>Viewing/Editing Parameters and Functions</bold></para>
	
	<para>
	Parameters and functions can be viewed by clicking the 'Parameters and Functions' node in the 'Navigation' panel tree structure. This displays the parameters in the 'View' panel on the right.
	Clicking the <bold>Parameters and Functions</bold> tab will display the model parameters in the table. The table shows the name, description, parameter expression and units of each parameter. 
	In addition, the 'Defined in:' column indicates where the parameter is defined. 'Global' indicates that the parameter is a global parameter, defined at the physiology level. 
	'Model/Reaction(r1)' indicates that the parameter is defined in the model in reaction 'r1'; such a parameter cannot be used outside the reaction. 
	'App(application_name)' indicates that the parameter is defined in the application 'application_name'. Species initial conditions, compartment sizes, etc. fall under this category of parameters.
	</para>
	
	<para>
	There are 2 sets of checkboxes above the parameters table allows the user to filter the type of parameters being displayed. 
	</para>

	<list>
		<item> <bold>Defined in:</bold> filter the parameters based on where they are defined (as described above) : <bold>Global</bold>, <bold>Reactions</bold> or <bold>Applications</bold>. </item>
		<item> <bold>Type:</bold> filters the parameters based on parameter type. If the <bold>Parameters</bold> checkbox is checked, only parameters with numeric values are displayed in the table.
		If <bold>Functions</bold> checkbox is checked, only those parameters that are expressions of other allowable variables in the model are displayed in the parameters table.	
		</item>
	</list>
	
	<para>
	The user can edit parameter expressions and units by double-clicking the corresponding column in the table for the selected parameter. 
	Alternately, selecting a parameter or a set of parameters in the table and right-clicking on the table shows a popup menu that allows the user to edit the expression or unit for selected parameter(s) in a text field.
	<bold>Note:</bold> the units can be changed only for user-defined parameters.
	</para>
	
	<imgReference target = "ParamEditExpr.png"/>
	
	<para>
	The <bold>Search</bold> text field below the parameters table allows the user to search for parameters in the table based on the search string entered by the user. 
	The table is filtered to display those parameters that match the given search string. 
	</para>
	
	<para><bold>Adding and Deleting Global Parameters</bold></para>
	<para>
	Clicking the <bold>Add Global Parameter</bold> button adds a default global parameter to the table. The user can edit the newly added parameter by clicking on the corresponding column in the table. 
	Alternately, selecting the parameter in the table shows the <link target="PP_Parameters">Parameters Properties Pane</link> in the 'Properties' panel below where the user can edit the parameter attributes.
	The <bold>Delete Selected Global Parameter(s)</bold> button is enabled only when the selection in the table is a global parameter. 
	Selecting a global parameter in the table and clicking on the 'Delete Selected Global Parameter(s)' button shows a warning dialog warning the user that the global parameters are going to be deleted. 
	</para>
	<imgReference target = "ParamDeleteWarning.png"/>
	<para>
	If any of the selected global parameters are used in other expressions in the model, clicking <bold>OK</bold> in the warning dialog pops up an error dialog listing those parameters that cannot be deleted. 
	If the selected parameters are not used elsewhere in the model, clicking 'OK' in the warning dialog removes the global parameter(s) from the model.  
	</para>
	<imgReference target = "ParamDeleteError.png"/>
	</operations>
	
</page>

</vcelldoc>