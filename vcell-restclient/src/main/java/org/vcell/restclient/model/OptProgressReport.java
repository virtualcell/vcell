/*
 * VCell API
 * VCell API
 *
 * The version of the OpenAPI document: 1.0.1
 * Contact: vcell_support@uchc.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.vcell.restclient.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import org.vcell.restclient.model.OptProgressItem;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;


/**
 * OptProgressReport
 */
@JsonPropertyOrder({
  OptProgressReport.JSON_PROPERTY_BEST_PARAM_VALUES,
  OptProgressReport.JSON_PROPERTY_PROGRESS_ITEMS
})
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class OptProgressReport {
  public static final String JSON_PROPERTY_BEST_PARAM_VALUES = "bestParamValues";
  private Map<String, Double> bestParamValues = new HashMap<>();

  public static final String JSON_PROPERTY_PROGRESS_ITEMS = "progressItems";
  private List<OptProgressItem> progressItems;

  public OptProgressReport() { 
  }

  public OptProgressReport bestParamValues(Map<String, Double> bestParamValues) {
    this.bestParamValues = bestParamValues;
    return this;
  }

  public OptProgressReport putBestParamValuesItem(String key, Double bestParamValuesItem) {
    if (this.bestParamValues == null) {
      this.bestParamValues = new HashMap<>();
    }
    this.bestParamValues.put(key, bestParamValuesItem);
    return this;
  }

   /**
   * Get bestParamValues
   * @return bestParamValues
  **/
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_BEST_PARAM_VALUES)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)

  public Map<String, Double> getBestParamValues() {
    return bestParamValues;
  }


  @JsonProperty(JSON_PROPERTY_BEST_PARAM_VALUES)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setBestParamValues(Map<String, Double> bestParamValues) {
    this.bestParamValues = bestParamValues;
  }


  public OptProgressReport progressItems(List<OptProgressItem> progressItems) {
    this.progressItems = progressItems;
    return this;
  }

  public OptProgressReport addProgressItemsItem(OptProgressItem progressItemsItem) {
    if (this.progressItems == null) {
      this.progressItems = new ArrayList<>();
    }
    this.progressItems.add(progressItemsItem);
    return this;
  }

   /**
   * Get progressItems
   * @return progressItems
  **/
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_PROGRESS_ITEMS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)

  public List<OptProgressItem> getProgressItems() {
    return progressItems;
  }


  @JsonProperty(JSON_PROPERTY_PROGRESS_ITEMS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setProgressItems(List<OptProgressItem> progressItems) {
    this.progressItems = progressItems;
  }


  /**
   * Return true if this OptProgressReport object is equal to o.
   */
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    OptProgressReport optProgressReport = (OptProgressReport) o;
    return Objects.equals(this.bestParamValues, optProgressReport.bestParamValues) &&
        Objects.equals(this.progressItems, optProgressReport.progressItems);
  }

  @Override
  public int hashCode() {
    return Objects.hash(bestParamValues, progressItems);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class OptProgressReport {\n");
    sb.append("    bestParamValues: ").append(toIndentedString(bestParamValues)).append("\n");
    sb.append("    progressItems: ").append(toIndentedString(progressItems)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    // add `bestParamValues` to the URL query string
    if (getBestParamValues() != null) {
      for (String _key : getBestParamValues().keySet()) {
        joiner.add(String.format("%sbestParamValues%s%s=%s", prefix, suffix,
            "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, _key, containerSuffix),
            getBestParamValues().get(_key), URLEncoder.encode(String.valueOf(getBestParamValues().get(_key)), StandardCharsets.UTF_8).replaceAll("\\+", "%20")));
      }
    }

    // add `progressItems` to the URL query string
    if (getProgressItems() != null) {
      for (int i = 0; i < getProgressItems().size(); i++) {
        if (getProgressItems().get(i) != null) {
          joiner.add(getProgressItems().get(i).toUrlQueryString(String.format("%sprogressItems%s%s", prefix, suffix,
          "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, i, containerSuffix))));
        }
      }
    }

    return joiner.toString();
  }
}

