package cbit.vcell.model;

import org.vcell.model.rbm.SpeciesPattern;

public class ReactantPattern extends ReactionRuleParticipant {
	
	public ReactantPattern(SpeciesPattern speciesPattern){
		super(speciesPattern);
	}
	
}
