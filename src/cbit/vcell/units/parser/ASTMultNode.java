/* Generated By:JJTree: Do not edit this line. ASTMultNode.java */

package cbit.vcell.units.parser;

import cbit.vcell.matrix.RationalNumber;

public class ASTMultNode extends SimpleNode {
  public ASTMultNode(int id) {
    super(id);
  }

  public ASTMultNode(UnitSymbolParser p, int id) {
    super(p, id);
  }

public String toInfix(RationalNumber power) {
	StringBuilder buffer = new StringBuilder();
	for (int i=0;i<jjtGetNumChildren();i++){
		if (i>0){
			buffer.append("*");
		}
		buffer.append(jjtGetChild(i).toInfix(power));
	}
	return buffer.toString();
}

public String toSymbol(RationalNumber power, UnitTextFormat format) {
	StringBuilder buffer = new StringBuilder();

	for (int i=0;i<jjtGetNumChildren();i++){
		if (i>0){
			buffer.append(".");
		}
		buffer.append(jjtGetChild(i).toSymbol(power,format));
	}
	return buffer.toString();
}

}
