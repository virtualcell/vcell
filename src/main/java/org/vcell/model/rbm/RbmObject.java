package org.vcell.model.rbm;

import org.vcell.util.Matchable;
import org.vcell.util.document.Identifiable;

// the base class for anything Rbm related (entities, processes)
public interface RbmObject {			// extends Identifiable, Matchable {

	
}
