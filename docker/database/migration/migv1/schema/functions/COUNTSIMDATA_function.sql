-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.3
-- Copyright 2000-2024 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=vcell-oracle.cam.uchc.edu;service_name=ORCLPDB1;port=1521

SET client_encoding TO 'UTF8';

SET search_path = vcell,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION vcell.countsimdata (userkey integer) RETURNS integer AS $body$
DECLARE

simDataCount integer;

BEGIN
  select count(*) into STRICT simDataCount from vc_simulation, vc_rsetMetaData
       where vc_simulation.ownerref = userkey
       and vc_simulation.id = vc_rsetMetaData.simref;
  return(simDataCount);
END;




$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION vcell.countsimdata (userkey integer) FROM PUBLIC;

