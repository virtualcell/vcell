-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.3
-- Copyright 2000-2024 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=vcell-oracle.cam.uchc.edu;service_name=ORCLPDB1;port=1521

SET client_encoding TO 'UTF8';

SET search_path = vcell,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION vcell.getvalencetype (valence text) RETURNS varchar AS $body$
DECLARE

     answer varchar(8);

BEGIN
     IF (valence is null) THEN
        answer := 'null';
     ELSE
        IF (valence = '0') THEN
           answer := 'zero';
        ELSE
           answer := 'nonzero';
        END IF;
     END IF;
     return answer;
END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION vcell.getvalencetype (valence text) FROM PUBLIC;

