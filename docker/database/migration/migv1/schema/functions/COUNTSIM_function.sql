-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.3
-- Copyright 2000-2024 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=vcell-oracle.cam.uchc.edu;service_name=ORCLPDB1;port=1521

SET client_encoding TO 'UTF8';

SET search_path = vcell,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION vcell.countsim (userkey integer) RETURNS integer AS $body$
DECLARE

simCount integer;

BEGIN
  select count(*) into STRICT simCount from vc_simulation where vc_simulation.ownerref = userkey;
  return(simCount);
END;






$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION vcell.countsim (userkey integer) FROM PUBLIC;

