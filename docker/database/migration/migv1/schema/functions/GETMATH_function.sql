-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.3
-- Copyright 2000-2024 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=vcell-oracle.cam.uchc.edu;service_name=ORCLPDB1;port=1521

SET client_encoding TO 'UTF8';

SET search_path = vcell,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION vcell.getmath (mathKey bigint, pos integer, amt integer) RETURNS varchar AS $body$
DECLARE

     mathCLOB text;
     buf varchar(32767);

BEGIN
     SELECT language INTO STRICT mathCLOB FROM vc_math WHERE id = mathKey;
     dbms_lob.open(mathCLOB, dbms_lob.lob_readonly);
     buf := '';
     buf := substr(mathCLOB, pos, amt);
     dbms_lob.close(mathCLOB);
     return buf;
END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION vcell.getmath (mathKey bigint, pos integer, amt integer) FROM PUBLIC;

