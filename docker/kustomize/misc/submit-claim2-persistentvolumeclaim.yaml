apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: submit-claim2
  name: submit-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
