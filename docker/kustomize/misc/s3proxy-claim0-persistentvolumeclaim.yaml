apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: s3proxy-claim0
  name: s3proxy-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
