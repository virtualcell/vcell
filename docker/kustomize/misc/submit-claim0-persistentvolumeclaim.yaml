apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: submit-claim0
  name: submit-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
