apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: submit-claim3
  name: submit-claim3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
