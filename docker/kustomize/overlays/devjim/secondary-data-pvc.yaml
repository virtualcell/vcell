apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: secondary-data-pvc
spec:
    accessModes:
    - ReadWriteMany
    storageClassName: standard
    resources:
        requests:
            storage: 100Mi
