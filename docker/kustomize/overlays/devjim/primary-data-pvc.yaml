apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: primary-data-pvc
spec:
    accessModes:
    - ReadWriteMany
    storageClassName: standard
    resources:
        requests:
            storage: 100Mi
